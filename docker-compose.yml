services:
  todo:
    container_name: todo
    image: nickfedor/todo:latest
    # image: ghcr.io/nicholas-fedor/todo:latest
    ports:
      - "3000:3000"
    volumes:
      - todo-data:/data
    restart: unless-stopped

volumes:
  todo-data:
